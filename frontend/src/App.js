import React from 'react';
// import { Routes, Route } from 'react-router-dom';

const App = () => {
    return (
        <div className="app">
            <h1>My App</h1>
            <p>This is a simple React app.</p>
            <p>It uses React Router for routing.</p>
            <p>It also uses Redux for state management.</p>
            <p>And it has a simple CSS file for styling.</p>
            {/* <Routes>
                <Route path="/" element={<>HELLO</>} />
            </Routes> */}
        </div>
    );
};

export default App;
